{
  "name": "Application-base",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "coffee --compile --output . . && node_modules/mocha/bin/_mocha --ui tdd -R spec -t 5000",
    "coverage": "node_modules/istanbul/lib/cli.js cover --hook-run-in-context node_modules/mocha/bin/_mocha -- --ui tdd -R spec -t 5000"
  },
  "dependencies": {
    "express": "3.4.x",
    "hogan-express": "0.5.x",
    "coffee-script": "1.7.x",
    "mongoose": "3.8.x"
  },
  "devDependencies": {
    "mocha": "1.17.x",
    "istanbul": "0.2.x",
    "nodemon": "1.0.x",
    "coffeelint": "1.0.x",
    "grunt-cli": "~0.1.13",
    "grunt": "~0.4.2",
    "request": "2.34.x"
  }
}
